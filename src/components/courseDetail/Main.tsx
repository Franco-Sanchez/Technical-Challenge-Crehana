import styled from 'styled-components'

function Main() {
  const StyledMain = styled.main`
    width: 85%;
    margin: 0 auto;
  `

  return (
    <StyledMain></StyledMain>
  )
}

export default Main;